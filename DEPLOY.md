<div dir="rtl">

# 🚀 راهنمای گام‌به‌گام انتشار روی گیت‌هاب

این راهنما برای کسی نوشته شده که **هیچ آشنایی قبلی با گیت‌هاب ندارد**. مرحله‌به‌مرحله جلو بروید.

هدف نهایی: فرم شما روی یک آدرس اینترنتی مثل زیر بالا بیاید و از هر گوشی و کامپیوتری بازش کنید:

```
https://نام-کاربری-شما.github.io/karamouzi-form/
```

</div>

---

<div dir="rtl">

## 📌 روش اول — بدون نصب هیچ چیز (پیشنهاد می‌شود)

این روش کاملاً از داخل مرورگر انجام می‌شود و حدود ۵ دقیقه طول می‌کشد.

### گام ۱ — ساخت حساب گیت‌هاب

۱. به آدرس <https://github.com/signup> بروید
۲. ایمیل، رمز عبور و یک نام کاربری انتخاب کنید
   - نام کاربری با حروف انگلیسی باشد، مثلاً `maryam-ahmadi`
   - **این نام در آدرس سایت شما ظاهر می‌شود، پس ساده انتخابش کنید**
۳. ایمیل خود را تأیید کنید

> اگر از قبل حساب دارید، این گام را رد کنید.

---

### گام ۲ — ساخت مخزن (Repository)

«مخزن» یعنی پوشه‌ی پروژه‌ی شما روی گیت‌هاب.

۱. وارد حساب خود شوید
۲. بالا سمت راست روی علامت **`+`** بزنید و **`New repository`** را انتخاب کنید
۳. فرم را این‌طور پر کنید:

| فیلد | مقدار |
|---|---|
| **Repository name** | `karamouzi-form` |
| **Description** | `فرم گزارش هفتگی کارآموزی` (اختیاری) |
| **Public / Private** | حتماً **Public** — چون GitHub Pages رایگان فقط روی Public کار می‌کند |
| **Add a README file** | تیک **نزنید** |

۴. پایین صفحه روی دکمه‌ی سبز **`Create repository`** بزنید

---

### گام ۳ — آپلود فایل‌ها

حالا صفحه‌ای می‌بینید که می‌گوید مخزن خالی است.

۱. روی لینک **`uploading an existing file`** کلیک کنید
   (اگر این لینک را نمی‌بینید: تب **`Add file`** ← **`Upload files`**)

۲. این فایل‌ها را با هم بکشید و داخل کادر رها کنید:

```
index.html      ← فایل اصلی (الزامی)
README.md       ← توضیحات پروژه
DEPLOY.md       ← همین راهنما
LICENSE         ← مجوز
.gitignore
```

۳. پایین صفحه در کادر **`Commit changes`** یک توضیح کوتاه بنویسید، مثلاً:
   ```
   افزودن فرم گزارش هفتگی کارآموزی
   ```

۴. روی دکمه‌ی سبز **`Commit changes`** بزنید

✅ فایل‌ها آپلود شدند.

---

### گام ۴ — روشن کردن GitHub Pages

اینجاست که پروژه تبدیل به یک سایت واقعی می‌شود.

۱. بالای صفحه‌ی مخزن، روی تب **`Settings`** (چرخ‌دنده) بزنید
۲. از منوی سمت چپ، روی **`Pages`** کلیک کنید
۳. در بخش **`Build and deployment`**:

| گزینه | مقدار |
|---|---|
| **Source** | `Deploy from a branch` |
| **Branch** | `main` |
| **Folder** | `/ (root)` |

۴. روی **`Save`** بزنید

---

### گام ۵ — دیدن سایت

۱. حدود **۱ تا ۲ دقیقه** صبر کنید
۲. همان صفحه‌ی `Settings ← Pages` را رفرش کنید
۳. یک کادر سبز با آدرس سایت شما ظاهر می‌شود:

```
Your site is live at https://نام-کاربری-شما.github.io/karamouzi-form/
```

۴. روی **`Visit site`** بزنید

🎉 **تمام!** حالا این آدرس را روی گوشی هم باز کنید و به صفحه‌ی اصلی اضافه‌اش کنید.

</div>

---

<div dir="rtl">

## 💻 روش دوم — با دستورات Git

اگر ترجیح می‌دهید از خط فرمان استفاده کنید:

### نصب Git

- **ویندوز:** از <https://git-scm.com/download/win> دانلود و نصب کنید
- **مک:** در ترمینال بزنید `git --version` و اجازه دهید نصب شود
- **لینوکس:** `sudo apt install git`

### دستورات

</div>

```bash
# ۱. وارد پوشه‌ی پروژه شوید
cd مسیر/پوشه/karamouzi-form

# ۲. مخزن را راه‌اندازی کنید
git init
git branch -M main

# ۳. هویت خود را ثبت کنید (فقط بار اول)
git config user.name "نام شما"
git config user.email "ایمیل-گیت‌هاب-شما@example.com"

# ۴. فایل‌ها را اضافه و ثبت کنید
git add .
git commit -m "افزودن فرم گزارش هفتگی کارآموزی"

# ۵. مخزن آنلاین را وصل کنید
#    (به‌جای USERNAME نام کاربری خودتان را بگذارید)
git remote add origin https://github.com/USERNAME/karamouzi-form.git

# ۶. آپلود کنید
git push -u origin main
```

<div dir="rtl">

> **نکته:** موقع `git push` گیت‌هاب رمز عبور معمولی را قبول نمی‌کند.
> باید یک **Personal Access Token** بسازید:
> `Settings` ← `Developer settings` ← `Personal access tokens` ← `Tokens (classic)` ← `Generate new token`
> دسترسی `repo` را تیک بزنید و توکن ساخته‌شده را به‌جای رمز عبور وارد کنید.

بعد از push، گام ۴ و ۵ از روش اول را انجام دهید تا سایت فعال شود.

</div>

---

<div dir="rtl">

## 🔄 به‌روزرسانی فایل در آینده

اگر بعداً فایل را تغییر دادید:

**از داخل مرورگر:**
۱. وارد مخزن شوید و روی `index.html` کلیک کنید
۲. آیکون مداد (✏️) بالا سمت راست را بزنید
۳. تغییرات را اعمال و `Commit changes` بزنید

**یا فایل جدید را جایگزین کنید:**
`Add file` ← `Upload files` ← فایل جدید را رها کنید ← `Commit changes`

سایت خودش ظرف یکی دو دقیقه به‌روز می‌شود.

**با Git:**

</div>

```bash
git add .
git commit -m "به‌روزرسانی فرم"
git push
```

---

<div dir="rtl">

## 📲 نصب روی گوشی مثل اپلیکیشن

بعد از اینکه سایت بالا آمد:

**اندروید (کروم):**
۱. آدرس سایت را باز کنید
۲. منوی سه‌نقطه (⋮) بالا سمت راست
۳. **`Add to Home screen`** یا **«افزودن به صفحه اصلی»**

**آیفون (سافاری):**
۱. آدرس سایت را باز کنید
۲. دکمه‌ی اشتراک‌گذاری (□↑) پایین صفحه
۳. **`Add to Home Screen`**

حالا یک آیکون روی صفحه‌ی گوشی دارید که مستقیم فرم را باز می‌کند.

</div>

---

<div dir="rtl">

## 🔒 نکته‌ی امنیتی هنگام آپلود

وقتی پروژه را روی گیت‌هاب می‌گذارید:

- ✅ **فایل `index.html` را با خیال راحت آپلود کنید** — این فقط فرم خالی است و هیچ اطلاعاتی از شما ندارد
- ❌ **هرگز فایل‌های `karamouzi-*.json` را آپلود نکنید** — این فایل‌ها کد ملی، شماره تماس و امضای شما را دارند

فایل `.gitignore` طوری تنظیم شده که این فایل‌ها به‌طور خودکار نادیده گرفته شوند، ولی اگر با روش
«Upload files» در مرورگر کار می‌کنید، خودتان حواستان باشد که فقط فایل‌های زیر را بکشید و رها کنید:

```
index.html      ✅
README.md       ✅
DEPLOY.md       ✅
LICENSE         ✅
.gitignore      ✅
```

> اطلاعاتی که در فرم وارد می‌کنید در مرورگر شما می‌ماند و به گیت‌هاب نمی‌رود.
> سایت منتشرشده برای هر بازدیدکننده یک فرم خالی است.

---

## ❓ مشکلات رایج

| مشکل | راه‌حل |
|---|---|
| **سایت باز نمی‌شود / خطای 404** | ۲ تا ۵ دقیقه صبر کنید. مطمئن شوید فایل حتماً `index.html` نام دارد (نه `Index.html`) و در ریشه‌ی مخزن است، نه داخل پوشه |
| **بخش Pages را در Settings نمی‌بینم** | مخزن باید **Public** باشد. از `Settings` ← پایین صفحه ← `Change repository visibility` عوضش کنید |
| **فونت درست نمایش داده نمی‌شود** | فونت داخل فایل جاسازی شده؛ یک‌بار `Ctrl+F5` بزنید تا کش پاک شود |
| **اطلاعاتم پاک شد** | اطلاعات در حافظه‌ی مرورگر است. اگر کش مرورگر را پاک کنید از بین می‌رود — عادت کنید هر هفته «خروجی JSON» بگیرید |
| **اطلاعات لپ‌تاپ در گوشی نیست** | این طبیعی است. با «خروجی JSON» و «بارگذاری JSON» منتقلشان کنید |
| **در گوشی نمی‌توانم چاپ بگیرم** | از منوی مرورگر گزینه‌ی `Print` را انتخاب کنید، سپس `Save as PDF` |

</div>

---

<div dir="rtl">

## 🔗 لینک‌های مفید

- [مستندات GitHub Pages](https://docs.github.com/pages)
- [آموزش مقدماتی Git](https://git-scm.com/book/fa/v2)

</div>
